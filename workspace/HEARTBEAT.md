# Heartbeat (optional)

Heartbeat runs are disabled in `deploy/openclaw.json` (`agents.defaults.heartbeat.every: "0m"`).

When you enable heartbeat in config, use this file to describe what you want checked each run (inbox triage, calendar skim, news digest, etc.).
